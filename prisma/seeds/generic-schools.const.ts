/**
 * Generic-варианты школ/колледжей, которые не нужны в выборе при регистрации.
 * Регионы с таким title (в любой локали) НЕ сидируются (`regions.seed.ts`)
 * и удаляются из БД (`delete-generic-schools.seed.ts`).
 *
 * Вынесено в отдельный модуль без сайд-эффектов: `regions.seed.ts` вызывает
 * `main()` на верхнем уровне, поэтому импортировать константу прямо из него
 * нельзя — это запустило бы весь seed.
 */
export const DENYLISTED_REGION_TITLES = [
    'Другая сельская школа',
    'Другой сельский колледж',
    'Другая городская школа',
    'Другой городской колледж',
] as const;
